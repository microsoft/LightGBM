task = predict

data = covtype.libsvm.binary.test

input_model= LightGBM_model.txt

# ! ATTENTION, avoid using this and check how to prevent; somehow during prediction one feature too much is detected
predict_disable_shape_check=true